PausableAnimation animation = new PausableAnimation();
animation.pause(); // Pause the animation
animation.play(); // Resume the animation
animation.isPaused(); // Check if the animation is paused
